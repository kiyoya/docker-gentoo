[Unit]
Description=drupal
After=docker-mariadb.service
Requires=docker-mariadb.service

[Service]
TimeoutStartSec=0
ExecStart=/usr/bin/docker start -a drupal
ExecStop=/usr/bin/docker stop drupal

[Install]
WantedBy=multi-user.target
